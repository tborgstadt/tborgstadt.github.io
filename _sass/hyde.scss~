/*
 *  __                  __
 * /\ \                /\ \
 * \ \ \___   __  __   \_\ \     __
 *  \ \  _ `\/\ \/\ \  /'_` \  /'__`\
 *   \ \ \ \ \ \ \_\ \/\ \_\ \/\  __/
 *    \ \_\ \_\/`____ \ \___,_\ \____\
 *     \/_/\/_/`/___/> \/__,_ /\/____/
 *                /\___/
 *                \/__/
 *
 * Designed, built, and released under MIT license by @mdo. Learn more at
 * https://github.com/poole/hyde.
 */

/* Contents
 *
 * Global resets
 * Sidebar
 * Container
 * Reverse layout
 * Themes */


/* Global resets
 *
 * Update the foundational and global aspects of the page */

html {
  font-family: "PT Sans", Helvetica, Arial, sans-serif;
}

@media (min-width: 48em) {
  html {
    font-size: 16px;
  }
}

@media (min-width: 58em) {
  html {
    font-size: 20px;
  }
}

/* Sidebar
 *
 * Flexible banner for housing site name, intro, and "footer" content. Starts
 * out above content in mobile and later moves to the side with wider viewports */

.sidebar {
  text-align: center;
  padding: 2rem 1rem;
  color: rgba(255,255,255,0.5);
  background-color: #202020;
}

@media (min-width: 48em) {
  .sidebar {
    position: fixed;
    top: 0;
    left: 0;
    bottom: 0;
    width: 15rem;
    text-align: left;
  }
}

/* Sidebar Title & Defaults */
.sidebar-about {
  margin-top: 0rem;
  font-family: Helvetica;
  font-size: 1.6rem;
  font-weight: 100;     
}
.sidebar-about a {
  color: #fff;
}

/* tagline */
.sidebar-about .tagline {
  font-size: .88rem;     
  font-weight: 300;
}

/* image and credit */
.sidebar-about .imgatt {
  display: block;
  line-height: 1;
  font-size: .44rem;
  font-weight: 100;
}
.sidebar-about .imgatt img {
  max-width: 100%;
  margin: 0 0 .2rem;
  border-radius: 6px;
}
.sidebar-about .imgatt a {              
  color: rgba(255, 255, 255, 0.5);
}

/* description */
.sidebar-about .descript {
  font-size: .78rem;     
  font-weight: 300;
}

/* copyright footer */
.copyright {
  font-size: 0.65rem;    
  font-weight: 300;
}

/* Navigation */
.sidebar-nav {
  margin-bottom: 1rem;
}
.sidebar-nav-item {
  color: #fff;
  display: block;
  line-height: 1.50;
}
a.sidebar-nav-item:hover,
a.sidebar-nav-item:focus {
  text-decoration: underline;
}
.sidebar-nav-item.active {
  font-weight: bold;
}

/* Fontawesome icons are text */
.sidebar .social-icons a {     
  color: rgba(255, 255, 255, 0.5);
  padding-right: 0.15em;
}

.sidebar .social-icons a:hover {
  text-decoration: none;
}


/* Sticky sidebar
 *
 * Add the `sidebar-sticky` class to the sidebar's container to affix it the
 * contents to the bottom of the sidebar in tablets and up */

@media (min-width: 48em) {
  .sidebar-sticky {
    position: absolute;
    right:  1rem;
    bottom: 1rem;
    left:   1rem;
  }
}


/* Container
 *
 * Align the contents of the site above the proper threshold with some margin-fu
 * with a 25%-wide `.sidebar` */

.content {
  padding-top:    4rem;
  padding-bottom: 4rem;
}

@media (min-width: 48em) {
  .content {
    max-width: 38rem;
    margin-left: 20rem;
    margin-right: 2rem;
  }
}

@media (min-width: 64em) {
  .content {
    margin-left: 22rem;
    margin-right: 4rem;
  }
}


/* Tags 
 *
 */

.tag-box {
  list-style: none;
  margin: 0;
  padding: 4px 0;
  overflow: hidden;
  *zoom: 1;
}

.tag-box:before, .tag-box:after {
  display: table;
  content: "";
  line-height: 0;
}

.tag-box:after {
  clear: both;
}

.tag-box.inline li {
  float: left;
  font-size: 14px;
  font-size: 0.875rem;
  line-height: 2.5;
}

.tag-box a {
  padding: 4px 6px;
  margin: 2px;
  background-color: #e6e6e6;
  -webkit-border-radius: 4px;
  -moz-border-radius: 4px;
  border-radius: 4px;
  text-decoration: none;
}

.tag-box a span {
  vertical-align: super;
  font-size: 10px;
  font-size: 0.625rem;
}

